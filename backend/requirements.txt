# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32   # загрузка файлов (фото товара) в админке

# Database
sqlalchemy==2.0.52
alembic==1.19.1
psycopg2-binary==2.9.12

# Validation & settings
pydantic==2.13.4
pydantic-settings==2.15.0

# Background tasks
celery==5.6.3
redis==5.2.1

# HTTP client (уведомления в Telegram/ВК)
httpx==0.28.1

# XML parsing (для CommerceML)
lxml==5.3.0

# Auth
bcrypt==4.2.1
PyJWT==2.13.0

# Мониторинг ошибок (опционально — включается, если задан SENTRY_DSN)
sentry-sdk[fastapi]==2.68.1

# Utils
python-dotenv==1.2.3
# База часовых поясов IANA для zoneinfo — python:3.12-slim (Debian) не содержит
# /usr/share/zoneinfo. Нужна, чтобы ZoneInfo("Europe/Moscow") работал на любом хосте
# (перевод времени заказа UTC → локальное время магазина при выгрузке в МойСклад).
tzdata==2025.2
