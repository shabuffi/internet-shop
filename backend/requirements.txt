# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.32   # загрузка файлов (фото товара) в админке

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Validation & settings
pydantic==2.10.3
pydantic-settings==2.6.1

# Background tasks
celery==5.4.0
redis==5.2.1

# HTTP client (уведомления в Telegram/ВК)
httpx==0.28.1

# XML parsing (для CommerceML)
lxml==6.1.1

# Auth
bcrypt==4.2.1
PyJWT==2.10.1

# Мониторинг ошибок (опционально — включается, если задан SENTRY_DSN)
sentry-sdk[fastapi]==2.62.0

# Utils
python-dotenv==1.0.1
