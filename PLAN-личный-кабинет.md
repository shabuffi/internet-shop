# План: Личный кабинет, ценообразование и доставка

Разбивка ТЗ начальника на этапы. Порядок учитывает зависимости: каждый следующий
опирается на предыдущие. Статусы: ⬜ не начато · 🟡 в работе · ✅ готово.

## Что просит ТЗ (кратко)
1. Личный кабинет + самостоятельная регистрация (email, телефон, тип заказчика; ИНН для ИП/ООО) + оферта 152-ФЗ.
2. Админка: список пользователей + персональная скидка −30%…+9% (по умолчанию +5%) от базовой цены МойСклад.
3. Цены: гость/не вошёл → база +10% (до копеек); вошёл → база с его персональной скидкой.
4. Чекаут: выбор получения (самовывоз / транспорт ТД «Инженер» / ТК); способ передаётся в МойСклад.
5. Заказ из кабинета → данные заказчика передаются в МойСклад.
6. Заказ без регистрации → запросить email, наименование заказчика, телефон.

---

## Этапы

### Этап 0 — Фавикон + лого ✅
Перекраска лого под цвет сайта, прозрачный favicon. **Сделано.**

### Этап 1 — Базовая наценка +10% для всех ⬜
Цена считается **на бэкенде** (нельзя обойти): `отображаемая = база × 1.10`, округление до копеек.
- Backend: наценка в `ProductOut` (список + карточка), та же цена в расчёте суммы заказа. Конфиг `DEFAULT_MARKUP_PERCENT=10`.
- Самостоятельный, сразу видимый кусок; закладывает «цена считается на сервере».
- *Зависимость:* на этапе 4 цена станет зависеть от пользователя.

### Этап 2 — Пользователь + регистрация + вход (ядро кабинета) ⬜
Делаем по образцу уже существующей админ-авторизации (bcrypt + JWT в httpOnly-куке, без localStorage).
- Модель `User`: email (уник.), phone, `customer_type` (физлицо/ИП/ООО), `inn` (обяз. для ИП/ООО), `password_hash`, `discount_percent` (по умолч. +5), `consent_at`, `created_at`. Миграция (+ регистрация в `migrations/env.py`).
- Эндпоинты `/api/v1/auth`: `register` (валидация: ИНН обязателен и 10/12 цифр для ИП/ООО, email/phone), `login`, `logout`, `me`. Кука `customer_token`.
- Frontend: `/register`, `/login`, `/account` (профиль + мои заказы), контекст авторизации в шапке.

### Этап 3 — Оферта + согласие на обработку ПД (152-ФЗ) ⬜
- Страница `/offer` с текстом политики обработки ПД.
- В регистрации — обязательный чекбокс согласия со ссылкой на оферту; фиксируем `consent_at`.
- *Нужен юридический текст* от заказчика (либо ставим шаблон-рыбу и дорабатываем).

### Этап 4 — Персональные цены для вошедших ⬜
- Ценообразование из этапа 1 учитывает пользователя: `adj = user.discount_percent`, иначе +10%.
- Цена считается на бэке по текущему пользователю (кука едет в SSR-запросах).
- *Внимание к кешу:* цены теперь персональные — нельзя отдавать общий кеш с ценой.

### Этап 5 — Админка: пользователи + скидки ⬜
- Раздел `/admin/users`: список (email, тип, ИНН, телефон, скидка, дата).
- Редактирование `discount_percent` с валидацией −30…+9 (дефолт +5). Эндпоинты `GET /admin/users`, `PATCH /admin/users/{id}`.

### Этап 6 — Чекаут: доставка + передача в МойСклад + гость/кабинет ⬜
- Выбор способа получения: самовывоз / транспорт ТД «Инженер» / ТК.
- Гость: поля email, наименование заказчика, телефон. Из кабинета: данные подтягиваются.
- `Order`: хранить `delivery_method` и данные заказчика.
- Выгрузка заказа (`commerceml_orders.py`): добавить способ доставки и данные заказчика (контрагента) в CommerceML-XML для МойСклад.

---

## Открытые вопросы (нужно решение заказчика/начальника)
1. **Цена в заказе для МойСклад:** писать базовую цену МойСклад или фактическую (с наценкой/скидкой клиента)?
2. **Подтверждение email при регистрации:** нужно ли письмо-подтверждение (требует настройки почты) или регистрация без подтверждения?
3. **Текст оферты/политики ПД:** дадите готовый юридический текст или ставим шаблон-рыбу для доработки?

## Порядок реализации
0 ✅ → 1 → 2 → 3 → 4 → 5 → 6. Этапы 1 и 2 можно вести параллельно; 4, 5, 6 требуют этапа 2.
