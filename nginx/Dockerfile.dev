# Дев-образ nginx с запечённым конфигом — обход бага Docker Desktop на macOS,
# когда конфиг, примонтированный одним файлом, даёт "pread() Resource deadlock".
FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
