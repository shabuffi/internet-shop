# Security TODO — internet-shop

Оставшиеся задачи. Закрытые переносим в раздел «Закрыто» с датой.

Последнее обновление: **2026-07-14**

## Открыто

### Требует действий вне кода (на стороне владельца/сервера)
- [ ] **C-1** Ротировать пароль `MOYSKLAD_PASSWORD` (был в открытом локальном `.env`, считать скомпрометированным) и удалить мёртвый блок `MOYSKLAD_*` + `SYNC_INTERVAL_SECONDS` из `.env`. _(на проде `.env.prod` этого секрета уже нет — проверено 14.07.)_
- [ ] **Прод-доступ (по итогам SSH-аудита 14.07):** подтвердить, что все **3** ключа в `authorized_keys` root — свои; подтвердить вход с `176.124.216.176` (13.07 15:56).
- [ ] _(опц.)_ Включить `ufw` (allow 22/80/443); убрать стале-строки `PasswordAuthentication yes`/`PermitRootLogin yes` из основного `sshd_config`; опц. дописать `COOKIE_SECURE=true` в `.env.prod.example`.

### Требует решения по коду (отложено по указанию заказчика)
- [ ] **M-1** `docker.sock` в `autoheal`: запинить образ по digest, рассмотреть docker-socket-proxy (только restart).

### Развёртывание готовых фиксов
- [x] ~~Задеплоить Stage 1 + Stage 3~~ — **ВЫПОЛНЕНО 2026-07-14** (commit `9947863`, alembic `d6e7f8a9b0c1`), пост-деплой проверки зелёные.

## Закрыто

- [x] **M-4** `COOKIE_SECURE`/`DEBUG`/`ALLOWED_ORIGINS`/`SECRET_KEY` — проверены на проде 14.07 (SSH): всё корректно, на бою не проблема.
- [x] **Прод-хост аудит** — выполнен 14.07 (SSH, read-only): признаков компрометации нет, защита из инцидента 13.07 держится (детали в AUDIT.md). Остались мелкие подтверждения (см. выше).
- [x] **B-1** Race condition номера заказа → Postgres SEQUENCE `order_number_seq` (Stage 3; 🚀 задеплоено 2026-07-14 `9947863`).
- [x] **B-2** Уникальность `users.phone` на уровне БД → unique-индекс `ix_users_phone` (Stage 3; 🚀 задеплоено 2026-07-14 `9947863`; дублей на проде не было).
- [x] **H-1/H-2/H-3/M-2/M-5** (Stage 1) — 🚀 задеплоены 2026-07-14 `9947863`, проверены на проде.
