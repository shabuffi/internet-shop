# Security Changelog — internet-shop

Хронология изменений по безопасности. Новые записи — сверху.

## 2026-08-04 — 🚀 ДЕПЛОЙ: безопасная смена email + подсказка об опечатках (commit `d945eed`, миграция `b3c4d5e6f7a8`)
- Прод: бэкап `backup_2026-08-04_103221.sql` (5.7M), alembic `ba73f69c6f92 → b3c4d5e6f7a8`, worker/beat пересозданы (новые задачи `send_email_change_confirm`/`notify_email_changed` зарегистрированы).
- Пост-деплой (всё ✅): 8 контейнеров up; `/`, `/catalog`, `/login`, `/register`, `/account`, `/email-confirm` → 200; `/docs` → 404; `check-email` отвечает подсказкой; почтовые настройки прода (`smtp_*`, `notify_email`, `vk_peer_id`) сверены до/после — идентичны; заявок на смену email 0, тестовых данных 0; ошибок и 5xx в логах нет.

## 2026-08-03 — Безопасная смена email + подсказка об опечатках (в коде, до деплоя)
- Ветка `claude/secure-email-change-validation-31eb3a`, миграция `b3c4d5e6f7a8` (колонки `users.pending_email`, `pending_email_at`; head стал единым).
- Смена email в два шага: заявка с текущим паролем → письмо со ссылкой на НОВЫЙ адрес (24 ч) → подтверждение меняет логин; на СТАРЫЙ адрес уходит письмо-сигнал. До подтверждения вход по прежнему адресу.
- **B-3 закрыт:** гонка регистраций больше не даёт 500 — `IntegrityError` → 409. Тот же гард на подтверждении смены email.
- Rate-limit: заявка 10/ч по IP **и 5/ч по аккаунту**, подтверждение 20/ч, `check-email` 120/ч.
- `POST /auth/check-email` — подсказка об опечатке в домене; занятость адреса не отдаёт (иначе перебор зарегистрированных адресов). Регистрацию не блокирует.
- Проверка: 230 тестов зелёные (+55 новых), `tsc --noEmit` чист. Прод не затронут.

## 2026-07-14 — Пост-деплой: инфраструктурные задачи (анализ, без изменений)
- Добавлен `ROLLBACK.md` — инструкция полного отката релиза `9947863` (git revert + alembic downgrade `d6e7→1567` + вариант восстановления из `backup_2026-07-14_180955.sql` + проверки). Ничего не выполнялось.

## 2026-07-14 — 🚀 ДЕПЛОЙ Stages 1–3 на прод (commit `9947863`)
- Запушено в `origin/main` (`0574788..9947863`), на проде `git pull && ./deploy.sh` (бэкап БД `backup_2026-07-14_180955.sql` 4.6M).
- Миграции применены: `1567fb6768ce → d5e6f7a8b9c0 → d6e7f8a9b0c1`. Дублей телефонов на проде не было (гард не сработал).
- **Пост-деплой проверки (все ✅):** `alembic current`=`d6e7f8a9b0c1`; health=`{"status":"ok"}`; все контейнеры up (healthy где есть healthcheck); `/docs`,`/openapi.json`,`/redoc`→404; сайт `/`→200; security-заголовки (HSTS/X-Frame-Options/nosniff/Referrer-Policy) присутствуют; обмен CommerceML отвечает в режиме «креды заданы» (`failure/Неверный логин или пароль`), МойСклад контактировал сразу после (`last_seen 18:11 UTC`); ошибок в логах нет.
- Задеплоено: H-1, H-2, H-3, M-2, M-5, B-1, B-2.

## 2026-07-14 — Аудит прод-хоста (SSH, read-only)
- Проверен `td-engineer.ru` (`31.129.110.22`): признаков компрометации нет, майнера нет, защита из инцидента 13.07 держится (SSH key-only, fail2ban, блок C2, non-root контейнеры, наружу только 22/80/443).
- **M-4 закрыт:** `.env.prod` корректен (`COOKIE_SECURE=true`, `DEBUG=false`, `ALLOWED_ORIGINS`=боевые HTTPS, `SECRET_KEY` задан, `MOYSKLAD_*` отсутствуют).
- Alembic на проде = `1567fb6768ce`; security-миграций там нет → линеаризация security-ветки поверх `1567fb6768ce` безопасна.
- Осталось подтвердить владельцу: 3 ключа в `authorized_keys`, вход с `176.124.216.176`.

## 2026-07-14 — Финальный review
- Создан `FINAL_REPORT.md` (итоговый отчёт: область, находки, статусы, оценка, рекомендации перед деплоем).
- Прогон всех тестов: устранены 3 регрессии в собственных изменениях (nextval на SQLite → dialect-фолбэк; тесты обмена под fail-closed → autouse-фикстуры; исчерпание rate-limit в тестах → autouse `_no_rate_limit`). **162 теста зелёные.**
- Миграции проверены: единый линейный head, downgrade/upgrade проходят, конфликтов нет. Лишних импортов/мёртвого кода нет.

## 2026-07-14 — Stage 3 (в коде, не задеплоено)
- B-1: race-free номер заказа через Postgres SEQUENCE `order_number_seq` (+миграция; dialect-фолбэк для SQLite-тестов).
- B-2: уникальность `users.phone` на уровне БД — unique-индекс `ix_users_phone` (+миграция, с гардом на дубли).

## 2026-07-14 — Документация
- Добавлен `DECISIONS.md` (история архитектурных/security-решений с мотивацией отвергнутых вариантов) — 8 решений аудита.
- В `CLAUDE.md` закреплено правило: после каждого этапа обновлять все 5 документов `docs/security/` до показа результатов.
- Заведена `docs/security/` (AUDIT.md, FIXES.md, TODO.md, CHANGELOG.md).

## 2026-07-14 — Stage 2 (проверка)
- Проверена прод-конфигурация (по коду/шаблону; `.env.prod` недоступен) и мёртвость `MOYSKLAD_*`. Изменений кода нет.

## 2026-07-14 — Stage 1 (в коде, не задеплоено)
- H-1 fail-closed обмена CommerceML.
- H-2 Swagger/OpenAPI/Redoc только при DEBUG.
- H-3 security-заголовки в nginx.prod.conf.
- M-2 rate-limit на /orders и /leads.
- M-5 /admin/setup под одноразовым SETUP_TOKEN (доработан после правки заказчика).

## 2026-07-14 — Аудит
- Проведён аудит безопасности (3 прохода). Подтверждено 10 находок (C-1, H-1..H-3, M-1/M-2/M-4/M-5, B-1/B-2); SQLi/XSS/IDOR/RCE/SSRF/CSRF и пр. — отсутствуют.
