{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=/tmp/.ccd_mem_reminder; if [ ! -f \"$f\" ] || [ $(( $(date +%s) - $(stat -f %m \"$f\" 2>/dev/null || echo 0) )) -gt 180 ]; then touch \"$f\"; printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"Напоминание себе: после заметных изменений кода запиши ЧТО и ЗАЧЕМ в память (~/.claude/projects/-Users-sshabunova-dev-internet-shop/memory/project-current-initiative.md); новые правила работы — в отдельный feedback-файл + строку в MEMORY.md. Не дублировать существующее.\"}}'; fi"
          }
        ]
      }
    ]
  }
}
